# Agent Rules

## Versioning

Every time you edit any file in this repository, you MUST increment the `version` field in `burning-chrome/manifest.json`.

This is mandatory for every change, no exceptions.
